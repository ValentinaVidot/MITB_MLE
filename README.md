[ValentinaVidot/MITB_MLE github repo](https://github.com/ValentinaVidot/MITB_MLE)

## Run with Docker Compose

Build the images and start Airflow with:

```bash
docker-compose build
docker-compose up
```

After the webserver starts, open:

```text
http://localhost:8080
```

Default login:

```text
username: admin
password: admin
```
